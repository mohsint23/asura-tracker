# Email sender address (use your custom domain for MailChannels)
NOTIFICATION_EMAIL=notifications@yourdomain.com

# Only needed if using Resend as fallback (see README)
# RESEND_API_KEY=re_your_api_key_here
